pestaña = "App 6";
console.log(pestaña);

const stay = canasta.every(producto => producto.precio < 500);
console.log(stay);

const stay2 = canasta.some(producto => producto.precio < 500);
console.log(stay);

